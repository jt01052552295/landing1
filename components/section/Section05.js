import React from 'react';
import PropTypes from 'prop-types';

const Section05 = (props) => {
  return (
    <section>
      <div className="fd-container-fluid">swipe</div>
    </section>
  );
};

Section05.propTypes = {};

export default Section05;
