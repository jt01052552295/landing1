import React from 'react';
import PropTypes from 'prop-types';

const Section03 = (props) => {
  return (
    <section>
      <div className="fd-container-fluid">slide</div>
    </section>
  );
};

Section03.propTypes = {};

export default Section03;
