import React from 'react';
import PropTypes from 'prop-types';

const Section04 = (props) => {
  return (
    <section>
      <div className="fd-container-fluid">music player</div>
    </section>
  );
};

Section04.propTypes = {};

export default Section04;
